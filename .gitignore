# Dependencias
node_modules/
frontend/node_modules/
backend/node_modules/

# Builds
frontend/dist/
backend/dist/

# Audio cache (puede ser grande)
backend/audio_cache/
audio_cache/

# Entorno
.env
frontend/.env
backend/.env

# Vite
frontend/.vite/

# Logs
*.log
npm-debug.log*

# Sistema
.DS_Store
Thumbs.db

# Scripts con keys — no subir al repo
scripts/upload-secrets.mjs

# Local runtime JSON databases & caches (Survives git pull & git checkout)
backend/data/*.json
backend/data/**/*.json

# Android / Gradle / Chaquopy build outputs
android/build/
android/app/build/
android/.gradle/
.gradle/
android/local.properties
android/.idea/
android/.cxx/
*.iml

# Python cache & artifacts
__pycache__/
*.py[cod]
*$py.class

# Clon local del repo del Space de Hugging Face — es un repo git aparte,
# nunca debe trackearse dentro de este (se coló como gitlink por un
# `git add -A` accidental, ver commit que elimina HF-kokoMusic-space).
HF-kokoMusic-space/


# Builds de Electron y el repo aparte de KokoMusic-lite
release/
backendKokoMusic/
